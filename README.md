```
Clone The git,
then go to te application directory and call CMD,
then folow those command
composer install
composer update
 npm install,
 npm update
php artisan key:generate
php artisan storage:link

then upload database from  SQL folder to you host
and finally call php artisan serve


```
